# متغيرات البيئة المطلوبة في Vercel

**تاريخ:** 2026-01-30

---

## 🔐 متغيرات البيئة الأساسية

### 1. متغيرات التطبيق
```
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=https://your-domain.vercel.app
```

### 2. متغيرات الأمان
```
JWT_SECRET=your-strong-secret-key-here-min-32-chars
ENCRYPTION_KEY=your-encryption-key-here-min-32-chars
```

### 3. متغيرات الحساب
```
ADMIN_EMAIL=admin@reviewqeem.com
ADMIN_PASSWORD=your-secure-password
```

### 4. متغيرات npm (اختياري - لتجاوز تحذيرات الأمان)
```
NPM_CONFIG_AUDIT=false
```

---

## 📝 كيفية إضافة المتغيرات في Vercel

### الخطوات:
1. اذهب إلى **Project Settings** → **Environment Variables**
2. أضف كل متغير على حدة:
   - **Key:** اسم المتغير (مثلاً `JWT_SECRET`)
   - **Value:** القيمة (مثلاً `your-secret-key`)
   - **Environment:** اختر `Production`, `Preview`, `Development` (أو الكل)
3. اضغط **Save**
4. **أعد النشر** (Redeploy) للمشروع

---

## ⚠️ ملاحظات مهمة

### NPM_CONFIG_AUDIT=false
- **الغرض:** تجاوز تحذيرات الأمان في npm audit أثناء البناء
- **الاستخدام:** فقط إذا كانت تحذيرات npm audit تمنع البناء
- **التحذير:** لا يحل مشاكل الأمان، فقط يتجاهل التحذيرات

### JWT_SECRET و ENCRYPTION_KEY
- **يجب أن يكونا:** قويين وعشوائيين (32 حرف على الأقل)
- **لا تشاركهما:** أبداً في الكود أو GitHub
- **استخدم:** مولدات عشوائية قوية

---

## ✅ قائمة التحقق

- [ ] `NODE_ENV=production`
- [ ] `NEXT_PUBLIC_SITE_URL` (رابط الموقع الكامل)
- [ ] `JWT_SECRET` (مفتاح قوي)
- [ ] `ENCRYPTION_KEY` (مفتاح قوي)
- [ ] `ADMIN_EMAIL` (بريد الأدمن)
- [ ] `ADMIN_PASSWORD` (كلمة مرور قوية)
- [ ] `NPM_CONFIG_AUDIT=false` (اختياري - فقط إذا لزم الأمر)

---

**ملاحظة:** بعد إضافة المتغيرات، يجب إعادة نشر المشروع (Redeploy) لتطبيق التغييرات.

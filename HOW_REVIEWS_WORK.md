# كيف يعمل نظام قراءة المراجعات

## 📁 هيكل التخزين

### 1. الملفات
- **الموقع**: `data/reviews.encrypted`
- **التشفير**: AES-256-CBC
- **التنسيق**: JSON مشفر

### 2. كيفية القراءة

```typescript
// 1. LocalStorage يقرأ الملف المشفر
const storage = new LocalStorage('reviews');
const data = storage.read(); // يفك التشفير تلقائياً

// 2. ReviewLocal يستخدم LocalStorage
const reviews = await ReviewLocal.find({ status: 'published' });

// 3. البحث بالطرق المختلفة:
- findBySlug(slug)  // البحث بالـ slug
- findById(id)      // البحث بالـ ID
- findOne(query)    // البحث بشرط معين
- find(query)       // البحث بشرط (يعيد مصفوفة)
```

## 🔄 تدفق البيانات

### عند قراءة مراجعة واحدة:
```
1. المستخدم يضغط على بطاقة → /reviews/[slug]
2. page.tsx يستدعي getReview(slug)
3. getReview يحاول:
   - ReviewLocal.findBySlug(slug)
   - ReviewLocal.findById(slug)
   - البحث في جميع المراجعات
4. ReviewLocal.findOne() يستدعي:
   - storage.findOne(query)
   - storage.read() يقرأ الملف المشفر
   - يفك التشفير
   - يبحث في البيانات
5. يعيد Review object
6. ReviewViewClient يعرض المراجعة
```

### عند قراءة قائمة المراجعات:
```
1. الصفحة الرئيسية → app/page.tsx
2. getFeaturedReviews() يستدعي:
   - ReviewLocal.find({ status: 'published' })
3. ReviewLocal.find() يستدعي:
   - storage.find(query)
   - storage.read() يقرأ الملف
   - يفك التشفير
   - يفلتر حسب status === 'published'
4. يعيد مصفوفة من Review objects
5. يعرضها في Grid
```

## 🛠️ الملفات المسؤولة

### 1. التخزين
- `lib/storage/localStorage.ts` - قراءة/كتابة الملفات المشفرة
- `data/reviews.encrypted` - الملف المشفر

### 2. النموذج
- `lib/models/ReviewLocal.ts` - منطق المراجعات

### 3. الصفحات
- `app/page.tsx` - الصفحة الرئيسية (آخر 10)
- `app/reviews/page.tsx` - قائمة جميع المراجعات
- `app/reviews/[slug]/page.tsx` - صفحة مراجعة واحدة

## ⚠️ المشاكل المحتملة

1. **الملف غير موجود**: سيتم إنشاء ملف جديد فارغ
2. **التشفير فاشل**: سيتم إنشاء ملف جديد
3. **Slug غير موجود**: سيتم البحث بـ ID
4. **البيانات فارغة**: سيعيد مصفوفة فارغة []

## ✅ الحلول المطبقة

1. ✅ البحث المتعدد (slug, id, case-insensitive)
2. ✅ معالجة الأخطاء
3. ✅ إنشاء ملفات جديدة تلقائياً
4. ✅ فك التشفير التلقائي
